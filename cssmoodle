/* Основні кольори: синій і жовтий */
:root {
    --primary-color: #0057b7;  /* синій */
    --secondary-color: #ffd700;  /* жовтий */
}

/* Кнопки */
.core-button, .button {
    background-color: var(--primary-color) !important;
    color: white !important;
    border-radius: 6px;
}

/* Активні елементи меню */
.menu-item-selected, .tabbar > ion-tab-button.tab-selected {
    background-color: var(--secondary-color) !important;
    color: black !important;
}

/* Заголовки */
h1, h2, h3 {
    color: var(--primary-color) !important;
    font-family: 'Arial', sans-serif !important;
}

/* Текст */
p, li, span, div {
    font-family: 'Arial', sans-serif !important;
}

/* Акценти */
a, .link {
    color: var(--secondary-color) !important;
}
